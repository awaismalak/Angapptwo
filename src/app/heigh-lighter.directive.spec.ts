import { HeighLighterDirective } from './heigh-lighter.directive';

describe('HeighLighterDirective', () => {
  it('should create an instance', () => {
    const directive = new HeighLighterDirective();
    expect(directive).toBeTruthy();
  });
});
